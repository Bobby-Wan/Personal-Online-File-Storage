Трите основни страници (засега)

GET index
{
    //Only filled when user is not authorized (no cookie)
    error: string 
}

GET login
{
    //When user is already logged in, will redirect to home page
    error: string
}

GET signup
{
    //When user is already logged in, will redirect to home page
    error: string
}

Основни заявки (главно свързани с качване, сваляне на файлове)
POST upload-file (ще се прекръсти на upload)
BODY
{
    data:
        {
            
        }
    error:string
}

